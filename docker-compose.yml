services:
  svelte-kit:
    working_dir: /website
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dev-mc-scrpt-live
    entrypoint: /bin/sh
    ports:
      - 3000:3000 # Vite
      - 4173:4173 # Playwright
    volumes:
      - ./:/website/
    tty: true