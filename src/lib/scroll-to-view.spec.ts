import { describe, test } from 'vitest';
// import { scrollToView } from './scroll-to-view';

// TODO: Update test to work with vitest
describe('Test scrollToView function', async () => {
	test('Scroll to view', () => {
		/*
    // create a new element to scroll to
    const el = document.createElement('div');
    el.setAttribute('id', 'test');
    document.body.appendChild(el);

    // call scrollToView and wait for it to complete
    const scroll = scrollToView(el);
    await new Promise((resolve) => setTimeout(resolve, 1000));

    // check that the element has been scrolled to
    const targetElement = document.querySelector('#test');
    expect(targetElement?.getBoundingClientRect().top).toBeCloseTo(0, 0);

    // destroy the scroll object
    if (scroll) scroll.destroy();

    // clean up the test element
    document.body.removeChild(el);
*/
	});
});
